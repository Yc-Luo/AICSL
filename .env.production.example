# =============================================================================
# AICSL 生产环境配置模板
# =============================================================================
# 使用说明:
#   1. 复制此文件为 .env.production
#   2. 替换所有 <CHANGE_ME> 为实际值
#   3. 确保 .env.production 已添加到 .gitignore
#   4. 使用: docker-compose -f docker-compose.production.yml --env-file .env.production up -d
# =============================================================================

# -----------------------------------------------------------------------------
# 基础配置
# -----------------------------------------------------------------------------
# 应用版本标签
APP_VERSION=1.0.0

# 您的生产域名 (不带协议前缀，例如: aicsl.example.com)
DOMAIN_NAME=<CHANGE_ME>

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 后端工作进程数 (建议: CPU核心数 * 2 + 1)
BACKEND_WORKERS=4

# -----------------------------------------------------------------------------
# 安全密钥 (必须修改!)
# 生成命令: openssl rand -hex 32
# -----------------------------------------------------------------------------
# JWT 认证密钥 (至少32字符)
JWT_SECRET_KEY=<CHANGE_ME_RUN_openssl_rand_-hex_32>

# 应用加密密钥 (至少32字符)
SECRET_KEY=<CHANGE_ME_RUN_openssl_rand_-hex_32>

# -----------------------------------------------------------------------------
# MongoDB 配置
# -----------------------------------------------------------------------------
# 数据库名称
MONGODB_DB_NAME=aicsl

# 最大连接池大小
MONGODB_MAX_POOL_SIZE=20

# MongoDB 管理员账户 (用于初始化)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=<CHANGE_ME_STRONG_PASSWORD>

# MongoDB 应用账户 (后端使用)
MONGO_APP_USERNAME=aicsl_app
MONGO_APP_PASSWORD=<CHANGE_ME_STRONG_PASSWORD>

# -----------------------------------------------------------------------------
# Redis 配置
# -----------------------------------------------------------------------------
# Redis 访问密码
REDIS_PASSWORD=<CHANGE_ME_STRONG_PASSWORD>

# -----------------------------------------------------------------------------
# MinIO 对象存储配置
# -----------------------------------------------------------------------------
# MinIO 管理员账户
MINIO_ROOT_USER=minio_admin
MINIO_ROOT_PASSWORD=<CHANGE_ME_STRONG_PASSWORD_MIN_8_CHARS>

# 存储桶名称
MINIO_BUCKET_NAME=aicsl-files

# -----------------------------------------------------------------------------
# JWT 认证配置
# -----------------------------------------------------------------------------
# 访问令牌过期时间 (分钟，1440 = 24小时)
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# -----------------------------------------------------------------------------
# AI/LLM 配置
# -----------------------------------------------------------------------------
# LLM 提供商 (openai, deepseek, ollama)
LLM_PROVIDER=deepseek

# DeepSeek 配置 (推荐，性价比高)
DEEPSEEK_API_KEY=<CHANGE_ME_YOUR_DEEPSEEK_API_KEY>
DEEPSEEK_MODEL=deepseek-chat

# OpenAI 配置 (可选，如果使用 DeepSeek 可留空)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# -----------------------------------------------------------------------------
# 可选高级配置
# -----------------------------------------------------------------------------
# CDN 基础 URL (如使用 CDN 加速静态资源)
# CDN_BASE_URL=https://cdn.yourdomain.com

# 文件上传限制 (字节)
# MAX_FILE_SIZE=52428800
# MAX_PROJECT_STORAGE=5368709120

# -----------------------------------------------------------------------------
# SSL 证书说明
# -----------------------------------------------------------------------------
# 请将 SSL 证书放置在 nginx/ssl/ 目录下:
#   - fullchain.pem  (证书链)
#   - privkey.pem    (私钥)
#
# 获取 Let's Encrypt 免费证书:
#   ./scripts/deploy.sh ssl
# -----------------------------------------------------------------------------
